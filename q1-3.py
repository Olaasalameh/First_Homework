L = ['Network', 'Bio', 'Programming', 'Physics', 'Music']

# Loop through each item in the list
for item in L:
    # Check if the item starts with 'B' (case insensitive)
    if item.startswith('B'):
        print(item)
